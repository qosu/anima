CHANGED: rawos/cli/frontdoor_entry.py — new, lockout-proof ForceCommand entrypoint
CHANGED: pyproject.toml — new console script `rawos-frontdoor = rawos.cli.frontdoor_entry:main`
CHANGED: rawos/cli/main.py — _resolve_frontdoor_binary(); frontdoor_install now requires
         `rawos-frontdoor` on PATH (errors clearly if missing) instead of plain `rawos`
CHANGED: tests/test_frontdoor_entry.py — 9 new tests
  WHY: `rawos frontdoor install` pointed ForceCommand at plain `rawos` (pip script,
       zero try/except around `from rawos.cli.main import main`). Any syntax/import
       error in cli/main.py would brick SSH on a fresh install — exactly Lockout #1/#2,
       day one, on any new VPS. Current box only survived because of a hand-written,
       non-git wrapper /usr/local/bin/rawos-frontdoor.
       Fix: frontdoor_entry.py wraps the cli.main import in try/except → bash fallback
       (SSH_ORIGINAL_COMMAND passthrough or interactive -bash). Now a proper console
       script, regenerated by `pip install -e .`, survives every future install.
DEPLOYED (this box): pip install -e . → venv/bin/rawos-frontdoor generated.
  /usr/local/bin/rawos-frontdoor: hand-written wrapper → symlink to venv/bin/rawos-frontdoor
  (old wrapper kept as /usr/local/bin/rawos-frontdoor.handwritten.bak).
  `rawos frontdoor install --revert-after 300` → sshd -t OK → passthrough verified →
  `rawos frontdoor commit` (disarmed).
VERIFY: pytest -q → 827 passed (was 818, +9 new, zero regressions)
VERIFY: ssh root@178.104.255.197 'echo ok' → passthrough OK, rawos.service active
NEXT: commit code (pyproject.toml, cli/main.py, frontdoor_entry.py, test file).
      M1 installer now safe-by-default — VPS rental for Phase 22/23/24 unblocked.
