CHANGED: rawos/kernel/tools.py, tests/test_tier_enforcement.py (commits ffab93e0, 31864421, 568efa28); rawos/config.py, rawos/scheduler/proactive.py, rawos/api/app.py, tests/test_self_probe.py (commit 552b752e); PLAN.md (this commit)
WHY: Phase 16 Pass 2 fully implemented — execute() now enforces the TIER 0/1/2 boundary for rawos self-modification (live tree hard-refused, worktrees detect-and-revert incl. commit-smuggling via git reset --soft, TIER1 bootstrap gating per Pass 1 item 2). Self-probe loop wired but DORMANT (settings.self_probe_enabled=False, manual enable per user decision). 193 tests pass, zero regressions.
NEXT: step e finish — restart rawos.service, confirm is-active + port 8002 + /metrics 200 (dormant loop must not break startup). After that, Phase 16 Pass 2 is CLOSED; no automatic follow-up — self-probe cycle implementation + flag flip is a future, human-initiated session after one manual worktree dry run.
